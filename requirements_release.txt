# 发布 / 分发环境依赖（消除 paddleocr OpenCV 版本警告）
# 使用场景：打包给非技术用户、内部交付、希望安装过程无冲突提示

numpy==2.0.2
pillow>=10,<12
mss>=9
PyAutoGUI>=0.9.54
pyperclip>=1.8
protobuf==3.20.2

# OCR 后端（建议至少 Paddle，RapidOCR 可选）
paddlepaddle==2.6.2
paddleocr==2.7.0.3
# 如需快速轻量，可再安装 rapidocr-onnxruntime（可选）
# rapidocr-onnxruntime==1.4.4

# 与 paddleocr 依赖上限兼容的 OpenCV 版本
opencv-contrib-python==4.6.0.66

# 可选扩展（按需开放注释）
# albumentations==2.0.8
# imgaug==0.4.0
# scikit-image==0.24.0
# scipy==1.13.1
# RapidFuzz==3.13.0
# Flask==3.1.2
# flask-babel==4.0.0
# PyMuPDF==1.20.2
# python-docx==1.2.0
# pdf2docx==0.5.8
# openpyxl==3.1.5

# 说明：
# 1. 若你只关心屏幕捕获 + OCR，保留上面“核心 + OCR + OpenCV”即可。
# 2. rapidocr-onnxruntime 可在 Paddle 初始化失败时作为备用；不需要可以不装。
# 3. 请勿同时安装 opencv-python 与 opencv-contrib-python。
